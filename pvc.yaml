kind: PersistentVolumeClaim     # PVC defines what capacity is to be claimed and mode
apiVersion: v1
metadata:
  name: pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
